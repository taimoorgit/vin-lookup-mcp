[project]
name = "vin-mcp"
version = "0.1.0"
description = "An MCP server that decodes vehicle VINs using the free NHTSA vPIC API."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false
